# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'InterviewTest' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

pod 'Kingfisher', '~> 5.0'
pod 'Alamofire', '~> 4.8.2'

  # Pods for InterviewTest

  target 'InterviewTestTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'InterviewTestUITests' do
    # Pods for testing
  end

end
